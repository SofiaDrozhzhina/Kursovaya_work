# 📚 КурсПлатформа — Flask + PostgreSQL

Система управления учебными курсами студентов с авторизацией, тремя ролями и расширенной статистикой.

---

## 🚀 Быстрый старт

### 1. Установить зависимости
```bash
pip install -r requirements.txt
```

### 2. Создать базу данных PostgreSQL
```sql
-- В psql или pgAdmin:
CREATE DATABASE courses_db;
```

### 3. Инициализировать схему
```bash
psql -U postgres -d courses_db -f init_db.sql
```

### 4. (Опционально) Заполнить демо-данными
```bash
python seed_demo.py
```

### 5. Запустить приложение
```bash
python app.py
```

Приложение будет доступно по адресу: **http://localhost:5000**

---

## ⚙️ Настройка подключения к БД

Через переменные окружения (или измените `DB_CONFIG` в `app.py`).

### Linux / macOS
```bash
export DB_HOST=localhost
export DB_PORT=5432
export DB_NAME=courses_db
export DB_USER=postgres
export DB_PASSWORD=postgres
export SECRET_KEY=your-secret-key
```

### Windows PowerShell
```powershell
$env:DB_HOST="localhost"
$env:DB_PORT="5432"
$env:DB_NAME="courses_db"
$env:DB_USER="postgres"
$env:DB_PASSWORD="postgres"
$env:SECRET_KEY="your-secret-key"
```

---

## 👤 Учётные записи

| Роль | Логин | Пароль |
|------|-------|--------|
| Администратор | `admin` | `admin123` |
| Преподаватель | `teacher1` | `pass123` |
| Преподаватель | `teacher2` | `pass123` |
| Студент | `student1` | `pass123` |
| Студент | `student2` | `pass123` |
| ... | ... | ... |

*(доступно после запуска `seed_demo.py`)*

---

## 📂 Структура проекта

```text
Kursovaya_work/
├── app.py                   # Основное Flask-приложение
├── init_db.sql              # SQL-схема PostgreSQL
├── seed_demo.py             # Скрипт заполнения демо-данными
├── docs_generator.py        # Генерация документов
├── requirements.txt         # Зависимости Python
├── README.md                # Этот файл
├── templates/
│   ├── base.html            # Базовый шаблон (sidebar, навигация)
│   ├── login.html           # Страница входа
│   ├── register.html        # Страница регистрации
│   ├── dashboard.html       # Дашборд
│   ├── users.html           # Управление пользователями (admin)
│   ├── students.html        # Список студентов
│   ├── teachers.html        # Список преподавателей
│   ├── courses.html         # Курсы
│   ├── grades.html          # Оценки
│   ├── statistics.html      # Расширенная статистика
│   ├── my_courses.html      # Мои курсы
│   ├── enroll.html          # Запись на курс (студент)
│   ├── my_grades.html       # Мои оценки (студент)
│   ├── documents.html       # Документы пользователя
│   └── admin_documents.html # Документы (admin)
├── static/
│   ├── uploads/
│   │   └── courses/         # Картинки курсов
│   └── signatures/          # Подписи для сертификатов
└── tests/
    └── test_app.py          # Модульные тесты
```

---

## 🔐 Роли и права доступа

### 👑 Администратор
- Полное управление пользователями
- Управление курсами (создать, редактировать, удалить)
- Просмотр и редактирование всех студентов и преподавателей
- Выставление и удаление оценок
- Полная статистика по всем разделам
- Работа с документами

### 🎓 Преподаватель
- Просмотр своих курсов и студентов
- Выставление оценок по своим курсам
- Просмотр статистики

### 🎒 Студент
- Запись на курсы
- Просмотр своих курсов
- Просмотр своих оценок
- Работа с личными документами

---

## 📈 Статистика

Страница статистики содержит 4 вкладки:

| Вкладка | Показатели |
|---------|-----------|
| Студенты | Всего, активных, ср./макс./мин. балл, топ-5 |
| Курсы | Всего, с преподавателем, ср. студентов, популярные |
| Преподаватели | Всего, ведут курсы, ср. курсов, топ-5 |
| Оценки | Распределение (отл./хор./удовл./неудовл.) + диаграмма |

---

## 🗄️ Схема базы данных

```text
users ─────────────┬── teachers ──── courses ──── enrollments ──── grades
                   └── students ─────────────────────────┘
```

- **users** — все пользователи (`admin`, `teacher`, `student`)
- **teachers** — расширение профиля преподавателя
- **students** — расширение профиля студента
- **courses** — учебные курсы
- **enrollments** — записи студентов на курсы
- **grades** — оценки за курсы

---

## 🧪 Тестирование

Для модульного тестирования используется `pytest`.

Запуск всех тестов:
```bash
pytest -v
```

Если тесты находятся в папке `tests/`, можно запускать так:
```bash
pytest tests -v
```



## 📄 Документы

В проекте предусмотрено формирование документов, связанных с учебным процессом.
Для этого используется отдельный модуль `docs_generator.py`.

В зависимости от роли пользователя доступны:
- просмотр документов;
- генерация сертификатов;
- работа с учебными документами.

---
---


> Чтобы кириллица в PDF отображалась корректно, рекомендуется положить шрифт `DejaVuSans.ttf` в папку `fonts/`.
> Если шрифт не найден, документ всё равно создаётся, но некоторые символы могут отображаться некорректно.

---
## ⚠️ Возможные проблемы при запуске

### Не удаётся подключиться к PostgreSQL
Проверьте:
- запущен ли сервер PostgreSQL;
- правильно ли указаны `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`;
- создана ли база данных `courses_db`.

### Не выполняется команда `psql`
Это означает, что PostgreSQL не добавлен в системную переменную `PATH`.
В таком случае можно:
- использовать полный путь к `psql`;
- выполнить `init_db.sql` через pgAdmin.

### Не создаются картинки курсов
Убедитесь, что установлен `Pillow`:
```bash
pip install Pillow
```

### Не запускаются тесты
Убедитесь, что установлен `pytest`:
```bash
pip install pytest
```

---

## 🛠️ Используемые технологии

- Python 3.10+
- Flask
- PostgreSQL
- psycopg2
- Werkzeug
- Jinja2
- python-docx
- ReportLab
- openpyxl
- Pillow
- pytest

---

## 👨‍💻 Автор проекта

Проект разработан в рамках курсовой работы по дисциплине, связанной с разработкой программных модулей.